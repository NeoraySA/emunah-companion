# Emunah Companion – Admin Panel

> **Status**: Placeholder – Next.js will be initialized in a dedicated feature branch.

## Setup (future)

```bash
npx create-next-app@latest . --typescript --tailwind --app --src-dir
```

## Tech Stack
- Next.js (App Router)
- TypeScript
- Tailwind CSS + shadcn/ui
- React Query (TanStack Query)
- React Hook Form + Zod

See `instructions/admin.instructions.md` for full conventions.
